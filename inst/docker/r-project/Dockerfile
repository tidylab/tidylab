# R Package Development: Project -----------------------------------------------
FROM tidylab/package:4.1.3

# Additional Packages ----------------------------------------------------------
RUN R -q -e "install_cran(c('janitor'), dependencies = TRUE)"
